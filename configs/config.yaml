project:
  seed: 42

paths:
  dataset_csv: data/raw/digital_payments_fraud.csv
  model_dir: models
  reports_dir: reports

data:
  target_col: fraud_label
  drop_cols: [transaction_id, user_id]
  test_size: 0.2

features:
  categorical:
    - transaction_type
    - payment_mode
    - device_type
    - device_location
    - is_international
  numeric:
    - transaction_amount
    - account_age_days
    - transaction_hour
    - previous_failed_attempts
    - avg_transaction_amount
    - ip_risk_score
    - login_attempts_last_24h
  one_hot_min_freq: 10

model:
  params:
    n_estimators: 2000
    max_depth: 3
    min_child_weight: 5
    learning_rate: 0.03
    subsample: 0.8
    colsample_bytree: 0.8
    reg_lambda: 2.0
    objective: binary:logistic
    eval_metric: aucpr

decision:
  threshold: 0.5

explain:
  shap_max_samples: 500
  top_k_features: 10
